{
  "name": "sela-restaurant",
  "version": "1.0.0",
  "private": true,
  "description": "Sela restaurant website and guest booking demo",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/assets/client.mjs && node --check api/*.mjs && node --check api/*.cjs && node --test",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  }
}
